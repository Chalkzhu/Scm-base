import { createContext, useContext } from 'react';

export default createContext();

// const Context = createContext();
// const cStore = createContext(new Store());
// const useStore = () => useContext(cStore);

